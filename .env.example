# Porta do servidor (Vercel usa automaticamente a variável PORT)
PORT=3000

# Token para acesso ao painel administrativo
ADMIN_TOKEN=troque_por_senha_forte_aqui

# Node environment
NODE_ENV=production
